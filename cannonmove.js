3$(function() {
    var position = $("#cannon").position(),
        mouseX, mouseY, cannonX, cannonY, degree;
        
        $("body").mousemove(function(event) {
            console.log(event);
        });
});